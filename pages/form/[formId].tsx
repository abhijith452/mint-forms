import type { NextPage } from 'next';
import styles from '../../styles/Form.module.css'

const Form: NextPage = () => {
 
    return(
        <div className={styles.form}>
hi
        </div>
    );
};

export default Form;
